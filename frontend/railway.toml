# Railway Frontend Service Configuration
# This file is in /frontend directory, so Railway should use it when Root Directory is /frontend

[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
outputDirectory = "dist"
staticAssetsPath = "dist"
# NO start command for static sites!

